<div class="w-full bg-white shadow-sm p-4">
    <div class="flex flex-row justify-between items-center">
        <a href="{{ route('admin.index') }}" class="text-lg font-bold text-purple-500">Administration</a>
        <ul class="flex flex-row space-x-2">
            <a href="{{ route('admin.login') }}" class="hover:bg-gray-100 p-2 rounded-lg text-gray-600">Login</a>
            <a href="{{ route('admin.login') }}" class="hover:bg-gray-100 p-2 rounded-lg text-gray-600">Return to Main
                Website</a>
        </ul>
    </div>
</div>
