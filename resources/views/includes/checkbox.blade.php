<div class="flex items-center mt-1 mx-2">
     <input type="checkbox" name="{{ $name }}" class="form-checkbox p-3 lg:p-2 text-jean" value="{{ $value }}" id="{{ $name }}">
     <label for="{{ $name }}" class="ml-2 text-sm text-gray-600"> {{ $label ?? '' }} </label>
</div>
