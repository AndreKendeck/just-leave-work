<div class="flex items-center mt-1">
     <input type="checkbox" name="{{ $name }}" value="{{ $value }}" class="form-checkbox text-jean p-3"
          id="{{ $name }}">
     <label for="{{ $name }}" class="mx-1 text-lg text-gray-600"> {{ $label ?? '' }} </label>
</div>